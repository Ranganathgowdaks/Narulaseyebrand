<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Login Form</title>
    <link rel="stylesheet" type="text/css" href="login.css">
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            background-color: #f7f7f7;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .login {
            width: 400px;
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            background-color: #fff;
        }

        .login h1 {
            text-align: center;
            color: #333;
            margin-bottom: 30px;
        }

        .login label {
            display: block;
            margin-bottom: 8px;
            color: #333;
            font-size: 16px;
        }

        .login input[type="email"],
        .login input[type="password"] {
            width: 100%;
            padding: 10px;
            margin-bottom: 20px;
            border: 1px solid #ccc;
            border-radius: 5px;
            box-sizing: border-box;
        }

        .login input[type="submit"] {
            width: 100%;
            padding: 10px;
            border: none;
            border-radius: 5px;
            background-color: #007bff;
            color: #fff;
            font-size: 16px;
            cursor: pointer;
        }

        .login input[type="submit"]:hover {
            background-color: #0056b3;
        }

        .login a {
            display: block;
            margin-top: 10px;
            color: #007bff;
            text-align: center;
            text-decoration: none;
        }

        .login a:hover {
            text-decoration: underline;
        }

        .login .logo {
            display: block;
            margin: 0 auto 20px;
            width: 150px;
            height: auto;
        }

        .login .form-group {
            margin-bottom: 20px;
        }

        .login .checkbox-group {
            display: flex;
            align-items: center;
        }

        .login .checkbox-group input {
            margin-right: 10px;
        }

        .login .checkbox-group span {
            font-size: 14px;
        }
    </style>
</head>
<body>
    <div class="login">
        {{!-- <img src="../public/narulas-logo.png" alt="Narulas Logo" class="logo"> --}}
        <h1>Login Form</h1>
        <form id="login" method="POST" onsubmit="return validateLogin()">
            <div class="form-group">
                <label for="Email">Email</label>
                <input type="email" name="Email" id="Email" placeholder="Email" required>
            </div>
            <div class="form-group">
                <label for="Pass">Password</label>
                <input type="password" name="pass" id="Pass" placeholder="Password" required>
            </div>
            <div class="checkbox-group">
                <input type="checkbox" id="check">
                <span>Remember me</span>
            </div>
            <input type="submit" name="log" id="log" value="Log In Here">
            <a href="#">Forgot Password</a>
            <a href="signup">Sign Up</a>
        </form>
    </div>

    <script>
        function validateLogin() {
            // Perform login validation here (e.g., check credentials)
            // For now, let's assume login is successful
            alert("Login successful!");
            return true; // Return false if you want to prevent the form from submitting
        }
    </script>
</body>
</html>
